# Screenshots

Add these here before publishing:

1. `scenario-overview.png` — full Make.com scenario view (the one you already have, showing Webhook → OpenAI → JSON → Airtable → Router → Slack)
2. `airtable-sample.png` — Airtable base showing a few logged leads with Score/Category/Next Action columns filled in
3. `slack-alert-sample.png` — the Slack message a hot lead triggers

Reference these in the README like:

```markdown
![Scenario overview](./docs/screenshots/scenario-overview.png)
```
